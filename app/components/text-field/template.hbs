<input type='text'
       id={{inputId}}
       name={{name}}
       placeholder={{placeholder}}
       autocomplete="off"
       onblur={{action 'blur'}}
       onpaste={{action 'reformat'}}
       onchange={{action 'reformat'}}
       oninput={{action 'reformat'}}
       disabled={{disabled}}>