{{#file-upload name=name accept=accept multiple=false onfileadd=onchange}}
  {{#liquid-if url}}
    {{yield (hash url=url name=value.name)}}
  {{else}}
    {{yield to='inverse'}}
  {{/liquid-if}}
{{/file-upload}}