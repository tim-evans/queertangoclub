{{#each value as |photo idx|}}
  {{photo-field value=photo name=(concat (or name inputId) '/' photo.id) onchange=(action 'change' idx)}}
{{/each}}
{{photo-field name=(concat (or name inputId) '/new') multiple=true onchange=(action 'add')}}
