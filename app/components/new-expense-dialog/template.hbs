{{#file-dropzone name='new-expense-photo'}}
  {{#form-for data onsubmit=(action 'submit') as |f|}}
    <header>{{icon 'broken-piggy-bank-solid'}} Add Expense</header>
    <div class="body">
      {{f.text 'description'}}
      <div class='flex-row half'>
        {{f.currency 'amount'}}
        {{f.date 'paidAt'}}
      </div>
      {{f.text 'paidBy'}}
      {{f.textarea 'notes' rows=3}}
      {{#f.photo 'receipt' name='new-expense-photo'}}
        {{icon 'add-solid'}} Add Receipt
      {{/f.photo}}
    </div>
    <footer>
      <button>Add Expense</button>
      {{#action-link action=dismiss}}Cancel{{/action-link}}
    </footer>
  {{/form-for}}
{{/file-dropzone}}
