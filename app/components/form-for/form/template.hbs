{{#if (is-array (get model fieldName))}}
  {{#each (get model fieldName) as |item idx|}}
    {{#form-for item index=idx tagName='div' oninit=(action 'registerWith' parent) onsubmit=onsubmit class=class as |f|}}
      {{yield f}}
    {{/form-for}}
  {{else}}
    {{yield to="inverse"}}
  {{/each}}
{{else}}
  {{#form-for (get model fieldName) tagName='div' oninit=(action 'registerWith' parent) onsubmit=onsubmit class=class as |f|}}
    {{yield f}}
  {{/form-for}}
{{/if}}
